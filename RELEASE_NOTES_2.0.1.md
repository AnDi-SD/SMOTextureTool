# SMOTextureTool 2.0.1 — Release candidate

This maintenance version adopts the common clean release layout and the shared
.NET 8 Desktop Runtime bootstrapper. Texture behavior is unchanged. The package
is prepared for manual validation before publication.
